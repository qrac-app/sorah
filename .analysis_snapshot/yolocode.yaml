tmux:
  session: sorah
  window: dev

services:
  frontend:
    setup: bun install
    command: bun run dev
    cwd: .
    port: 3000

  convex:
    command: CONVEX_AGENT_MODE=anonymous bun convex dev
    cwd: .
